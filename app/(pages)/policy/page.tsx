import BackButton from "@/app/components/BackButton";
import UserPolicy from "@/app/components/Policy";
import Tooltip from "@/app/ui/Tooltip";

export default function PolicyPage() {
  return (
    <section>
      <BackButton />

      <UserPolicy />
    </section>
  );
}
